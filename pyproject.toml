[project]
name = "k3s-lab"
description = "Proyecto de K3s con Ansible"
version = "0.1.0"

dependencies = [
  "docsible>=0.7.17",
  "kubernetes>=24.2.0",
  "PyYAML>=6.0.1",
  "jsonpatch>=1.33",
  "netaddr>=1.3.0",
]

requires-python = ">=3.12"

[tool.setuptools]
packages = []
py-modules = []
