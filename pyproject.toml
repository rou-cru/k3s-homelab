[project]
name = "k3s-lab"
description = "Proyecto de K3s con Ansible"
version = "0.1.0"

dependencies = [
  "docsible>=0.7.17",
  "molecule>=24.0.0",
  "molecule-plugins[vagrant]>=23.5.0",
  "python-vagrant>=1.0.0",
  "pytest-testinfra>=10.0.0",
  "kubernetes>=24.2.0",
  "PyYAML>=3.11",
  "jsonpatch",
  "netaddr>=1.3.0",
]

requires-python = ">=3.12"

[tool.setuptools]
packages = []
py-modules = []
