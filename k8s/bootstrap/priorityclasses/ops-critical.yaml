apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: ops-critical
value: 1000000
globalDefault: false
preemptionPolicy: PreemptLowerPriority
description: "IT Operations - Storage, security, certificates. Never preempted by workloads."
