# Plantilla de configuración secreta - copiar a secrets.yaml
# Copia este archivo a 'secrets.yaml' y rellena los valores.
# secrets.yaml está ignorado por git para tu seguridad.
tailscale_authkey: "tskey-auth-REEMPLAZA-ESTO-POR-TU-CLAVE-REAL"
