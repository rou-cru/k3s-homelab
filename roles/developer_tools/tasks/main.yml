# @docsible Installs base development packages (btop, jq, tmux)
- name: Install dev tools (apt)
  ansible.builtin.apt:
    name:
      - btop
      - bat
      - tree
      - tmux
      - jq
      - curl
      - python3-venv
      - gnupg
      - ca-certificates
    state: present
    update_cache: true
# @docsible Installs NVM (Node Version Manager)
- name: Install NVM
  ansible.builtin.shell: |
    set -euo pipefail
    curl -fsSL https://raw.githubusercontent.com/nvm-sh/nvm/{{ devtools_nvmVersion }}/install.sh | bash
  args:
    creates: /root/.nvm/nvm.sh
    executable: /bin/bash
  when: not ansible_check_mode
# @docsible Installs latest Node.js LTS via NVM
- name: Install latest Node.js LTS via NVM
  ansible.builtin.shell: |
    set -euo pipefail
    export NVM_DIR="/root/.nvm"
    . "${NVM_DIR}/nvm.sh"
    nvm install --lts
    nvm alias default 'lts/*'
  args:
    executable: /bin/bash
  when: not ansible_check_mode
# @docsible Enables Corepack and activates pnpm
- name: Enable Corepack and activate pnpm
  ansible.builtin.shell: |
    set -euo pipefail
    export NVM_DIR="/root/.nvm"
    . "${NVM_DIR}/nvm.sh"
    corepack enable
    corepack prepare pnpm@latest --activate
  args:
    executable: /bin/bash
  when: not ansible_check_mode
# @docsible Installs Devbox (Nix wrapper)
- name: Install Devbox
  ansible.builtin.shell: |
    curl -fsSL https://get.jetpack.io/devbox | bash -s -- -f
  args:
    creates: /usr/local/bin/devbox
  when: not ansible_check_mode
# @docsible Installs nvitop (GPU process monitor) via uv
- name: Install nvitop
  ansible.builtin.command: uv tool install nvitop
  args:
    creates: /root/.local/bin/nvitop
  environment:
    PATH: "/root/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"
  when:
    - nvidia_setupMode | default('auto') != 'false'
    - not ansible_check_mode
# @docsible Installs Kimi CLI (AI assistant) via uv
- name: Install Kimi
  ansible.builtin.command: uv tool install kimi-cli
  args:
    creates: /root/.local/bin/kimi
  environment:
    PATH: "/root/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"
  when: not ansible_check_mode
# @docsible Installs Global AI CLI tools (Claude, Gemini, OpenAI)
- name: Install AI tools
  ansible.builtin.shell: |
    set -euo pipefail
    export NVM_DIR="/root/.nvm"
    . "${NVM_DIR}/nvm.sh"
    npm install -g {{ item }}
  args:
    executable: /bin/bash
  loop:
    - "@anthropic-ai/claude-code"
    - "@google/gemini-cli"
    - "@qwen-code/qwen-code"
    - "@openai/codex"
  when: not ansible_check_mode
# @docsible Installs Docker CLI when devtools_installDocker is enabled
- name: Install Docker
  when:
    - devtools_installDocker | default(false) | bool
    - not ansible_check_mode
  block:
    # @docsible Adds Docker GPG key
    - name: Add Docker key
      ansible.builtin.shell: |
        set -o pipefail
        curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
          gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
      args:
        creates: /usr/share/keyrings/docker-archive-keyring.gpg
        executable: /bin/bash
    # @docsible Adds Docker APT repository
    - name: Add Docker repo
      ansible.builtin.apt_repository:
        repo: >-
          {{
            'deb [arch=' ~ (
              'amd64' if ansible_facts['architecture'] == 'x86_64'
              else 'arm64' if ansible_facts['architecture'] == 'aarch64'
              else 'unsupported'
            ) ~
            ' signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] ' ~
            'https://download.docker.com/linux/ubuntu ' ~
            ansible_facts['distribution_release'] ~
            ' stable'
          }}
        state: present
        filename: docker
    # @docsible Installs Docker CLI packages
    - name: Install Docker CLI
      ansible.builtin.apt:
        name:
          - docker-ce-cli
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
        update_cache: true
    # @docsible Warns about Docker/containerd co-existence
    - name: Show Docker warning
      ansible.builtin.debug:
        msg: "⚠️  Docker CLI installed (containerd used by K3s)"
  rescue:
    # @docsible Shows Docker install failure context
    - name: Report Docker installation failure
      ansible.builtin.debug:
        msg: >-
          Docker installation failed at {{ ansible_failed_task.name }}: {{ ansible_failed_result | to_json }}
    # @docsible Fails play when Docker CLI installation block errors
    - name: Fail Docker installation
      ansible.builtin.fail:
        msg: "Docker installation failed"
# @docsible Installs Vagrant and libvirt test stack when enabled
- name: Install Vagrant and libvirt
  when:
    - devtools_installVagrant | default(true) | bool
    - not ansible_check_mode
  block:
    # @docsible Resolves HashiCorp repo release codename
    - name: Set HashiCorp repo release
      ansible.builtin.set_fact:
        hashicorp_repo_release: >-
          {{ 'jammy' if ansible_facts['distribution_release'] == 'noble' else ansible_facts['distribution_release'] }}
    # @docsible Resolves HashiCorp repo architecture
    - name: Set HashiCorp repo architecture
      ansible.builtin.set_fact:
        hashicorp_repo_arch: >-
          {{
            'amd64'
            if ansible_facts['architecture'] == 'x86_64'
            else 'arm64'
            if ansible_facts['architecture'] == 'aarch64'
            else 'unsupported'
          }}
    # @docsible Validates architecture support
    - name: Validate HashiCorp repo architecture
      ansible.builtin.assert:
        that:
          - hashicorp_repo_arch != 'unsupported'
        fail_msg: "Unsupported architecture for HashiCorp repo: {{ ansible_facts['architecture'] }}"
    # @docsible Creates keyring directory
    - name: Ensure APT keyrings dir (HashiCorp)
      ansible.builtin.file:
        path: /usr/share/keyrings
        state: directory
        mode: "0755"
      when: not ansible_check_mode
    # @docsible Installs HashiCorp GPG key
    - name: Install HashiCorp keyring
      ansible.builtin.shell: |
        set -euo pipefail
        curl -fsSL https://apt.releases.hashicorp.com/gpg | \
          gpg --dearmor --batch --yes --no-tty -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
      args:
        executable: /bin/bash
        creates: /usr/share/keyrings/hashicorp-archive-keyring.gpg
      when: not ansible_check_mode
    # @docsible Adds HashiCorp APT repository
    - name: Add HashiCorp APT repo
      ansible.builtin.apt_repository:
        repo: >-
          {{
            'deb [arch=' ~ hashicorp_repo_arch ~
            ' signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] ' ~
            'https://apt.releases.hashicorp.com ' ~
            hashicorp_repo_release ~
            ' main'
          }}
        state: present
        filename: hashicorp
      when: not ansible_check_mode
    # @docsible Installs libvirt, qemu, and build deps
    - name: Install libvirt and dependencies
      ansible.builtin.apt:
        name:
          - qemu-kvm
          - libvirt-daemon-system
          - libvirt-clients
          - libvirt-dev
          - bridge-utils
          - virt-manager
          - gcc
          - make
          - pkg-config
          - ruby-dev
          - libxml2-dev
          - libxslt1-dev
          - zlib1g-dev
          - vagrant
        state: present
        update_cache: true
    # @docsible Starts libvirtd service
    - name: Ensure libvirtd service is enabled and started
      ansible.builtin.systemd:
        name: libvirtd
        enabled: true
        state: started
    # @docsible Adds ansible user to libvirt group
    - name: Add ansible_user to libvirt group
      ansible.builtin.user:
        name: "{{ ansible_user }}"
        groups: libvirt
        append: true
    # @docsible Adds root user to libvirt group
    - name: Add root to libvirt group
      ansible.builtin.user:
        name: root
        groups: libvirt
        append: true
    # @docsible Reads installed Vagrant plugins
    - name: Check installed Vagrant plugins
      ansible.builtin.command: vagrant plugin list
      register: vagrant_plugin_list
      changed_when: false
    # @docsible Installs vagrant-libvirt plugin
    - name: Install vagrant-libvirt plugin
      ansible.builtin.command: vagrant plugin install vagrant-libvirt
      register: vagrant_plugin_result
      changed_when: "'Installed the plugin' in vagrant_plugin_result.stdout"
      when:
        - not ansible_check_mode
        - vagrant_plugin_list.rc == 0
        - "'vagrant-libvirt' not in vagrant_plugin_list.stdout"
    # @docsible Prints Vagrant installation success message
    - name: Show Vagrant installation success
      ansible.builtin.debug:
        msg: "✅ Vagrant + libvirt installed successfully"
  rescue:
    # @docsible Shows Vagrant/libvirt install failure context
    - name: Report Vagrant installation failure
      ansible.builtin.debug:
        msg: >-
          Vagrant installation failed at {{ ansible_failed_task.name }}: {{ ansible_failed_result | to_json }}
    # @docsible Stops play after Vagrant/libvirt install failure
    - name: Fail Vagrant installation
      ansible.builtin.fail:
        msg: "Vagrant installation failed"
