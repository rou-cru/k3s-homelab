# @docsible Validate Tailscale IP format (100.x.x.x)
- name: Validate Tailscale IP
  ansible.builtin.assert:
    that:
      - IP_tailscale is defined
      - IP_tailscale | length > 0
      - IP_tailscale is match('^100\.')
    fail_msg: "ERROR: Invalid IP_tailscale ({{ IP_tailscale | default('undefined') }}). Must be 100.x.x.x"

# @docsible Verify required variables are defined
- name: Verify K3s agent requirements
  ansible.builtin.assert:
    that:
      - k3s_agentServerUrl is defined
      - k3s_agentServerUrl | length > 0
      - k3s_agentToken is defined
      - k3s_agentToken | length > 0
    fail_msg: "ERROR: k3s_agentServerUrl and k3s_agentToken are required"

# @docsible Check for gVisor runtime
- name: Check for runsc binary
  ansible.builtin.stat:
    path: /usr/local/bin/runsc
  register: k3s_runsc_stat

# @docsible Check for NVIDIA runtime
- name: Check for nvidia-container-runtime
  ansible.builtin.stat:
    path: /usr/bin/nvidia-container-runtime
  register: k3s_nvidia_runtime_stat

# @docsible Build list of additional containerd runtimes
- name: Build containerd runtime list
  ansible.builtin.set_fact:
    k3s_commonContainerdAdditionalRuntimes: >-
      {{
        []
        + (['runsc'] if k3s_runsc_stat.stat.exists else [])
        + (['nvidia'] if k3s_nvidia_runtime_stat.stat.exists else [])
      }}

# @docsible Import common K3s setup tasks
- name: Run K3s Common
  ansible.builtin.import_role:
    name: k3s_common
  vars:
    k3s_recreate: "{{ k3s_agentRecreate }}"
    k3s_commonRestartHandler: "Restart k3s-agent"

# @docsible Deploy agent configuration file
- name: Deploy agent config
  ansible.builtin.template:
    src: config.yaml.j2
    dest: /etc/rancher/k3s/config.yaml
    mode: "0600"
  notify: Restart k3s-agent
  when: not ansible_check_mode

# @docsible Install K3s agent binary
- name: Install K3s agent
  ansible.builtin.shell: |
    set -o pipefail
    curl -sfL https://get.k3s.io | \
      INSTALL_K3S_VERSION="{{ k3s_agentVersion }}" \
      INSTALL_K3S_EXEC="agent" \
      K3S_URL="{{ k3s_agentServerUrl }}" \
      K3S_TOKEN="{{ k3s_agentToken }}" \
      sh -
  args:
    executable: /bin/bash
    creates: /usr/local/bin/k3s
  no_log: true
  when: not ansible_check_mode

# @docsible Create systemd override directory
- name: Create override dir
  ansible.builtin.file:
    path: /etc/systemd/system/k3s-agent.service.d
    state: directory
    mode: "0750"

# @docsible Deploy systemd service override
- name: Create override
  ansible.builtin.copy:
    dest: /etc/systemd/system/k3s-agent.service.d/override.conf
    content: |
      [Service]
      ExecStart=
      ExecStart=/usr/local/bin/k3s agent
    mode: "0640"
  notify: Restart k3s-agent

# @docsible Reload systemd daemon
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

# @docsible Enable and start K3s agent service
- name: Start K3s agent
  ansible.builtin.systemd:
    name: k3s-agent
    enabled: true
    state: started
  when: not ansible_check_mode

# @docsible Apply pending handler restarts
- name: Flush handlers
  ansible.builtin.meta: flush_handlers

# @docsible Wait for node Ready condition
- name: Wait for node to be ready
  kubernetes.core.k8s_info:
    kind: Node
    name: "{{ ansible_facts['hostname'] }}"
    kubeconfig: "{{ kubeconfig }}"
  delegate_to: "{{ groups['k3s_server'][0] }}"
  register: node_info
  until: >-
    node_info.resources | length > 0 and
    (node_info.resources[0].status.conditions |
     selectattr('type', 'equalto', 'Ready') |
     map(attribute='status') |
     first) == 'True'
  retries: "{{ k3s_agentReadyzRetries }}"
  delay: "{{ k3s_agentReadyzDelay }}"
  when: not ansible_check_mode

# @docsible Apply custom node labels
- name: Apply node labels
  kubernetes.core.k8s:
    kind: Node
    name: "{{ ansible_facts['hostname'] }}"
    kubeconfig: "{{ kubeconfig }}"
    definition:
      metadata:
        labels: "{{ dict(k3s_agentNodeLabels | map('split', '=') | map('list')) }}"
  delegate_to: "{{ groups['k3s_server'][0] }}"
  when:
    - k3s_agentNodeLabels | length > 0
    - not ansible_check_mode

# @docsible Apply custom node taints
- name: Apply node taints
  kubernetes.core.k8s_taint:
    name: "{{ ansible_facts['hostname'] }}"
    kubeconfig: "{{ kubeconfig }}"
    taints:
      - key: "{{ item.split(':')[0].split('=')[0] }}"
        value: "{{ item.split(':')[0].split('=')[1] | default(omit) }}"
        effect: "{{ item.split(':')[1] }}"
    state: present
  delegate_to: "{{ groups['k3s_server'][0] }}"
  loop: "{{ k3s_agentNodeTaints }}"
  when:
    - k3s_agentNodeTaints | length > 0
    - not ansible_check_mode

# @docsible Display join confirmation
- name: Show agent join status
  ansible.builtin.debug:
    msg: "✅ K3s agent {{ ansible_facts['hostname'] }} joined cluster at {{ k3s_agentServerUrl }}"
