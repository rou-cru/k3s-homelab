# containerd-config.toml.tmpl.j2
# Custom containerd configuration for K3s agent nodes

{% set _default_runtime = k3s_commonContainerdDefaultRuntime | default('runc') %}

# Use optimized settings if enabled
{% if k3s_commonContainerdOptimized | default(true) %}
[plugins."io.containerd.grpc.v1.cri"]
  [plugins."io.containerd.grpc.v1.cri".containerd]
    default_runtime_name = "{{ _default_runtime }}"
    [plugins."io.containerd.grpc.v1.cri".containerd.runtimes]
      [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc]
        runtime_type = "io.containerd.runc.v2"
        [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options]
          SystemdCgroup = true
{% endif %}

{% if k3s_commonContainerdOptimized | default(true) %}
[plugins."io.containerd.grpc.v1.cri".registry]
  config_path = "/etc/rancher/k3s/registries.d"
{% endif %}

# Custom runtimes
[plugins."io.containerd.grpc.v1.cri".containerd.runtimes]
{% if k3s_commonContainerdOptimized | default(true) %}
  [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc]
    runtime_type = "io.containerd.runc.v2"
    [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options]
      SystemdCgroup = true
{% endif %}

{% if 'nvidia' in (k3s_commonContainerdAdditionalRuntimes | default([])) %}
  [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.nvidia]
    runtime_type = "io.containerd.runc.v2"
    [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.nvidia.options]
      BinaryName = "/usr/bin/nvidia-container-runtime"
      SystemdCgroup = true
{% endif %}

{% if 'runsc' in (k3s_commonContainerdAdditionalRuntimes | default([])) %}
  [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runsc]
    runtime_type = "io.containerd.runsc.v1"
{% endif %}

# High performance tuning
{% if k3s_commonContainerdOptimized | default(true) %}
[plugins."io.containerd.grpc.v1.cri"]
  max_container_log_line_size = 16384
{% endif %}
