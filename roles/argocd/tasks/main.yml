- name: Check if Helm is installed
  ansible.builtin.command: which helm
  register: helm_binary_check
  changed_when: false
  failed_when: false

- name: Ensure ArgoCD Helm repo
  kubernetes.core.helm_repository:
    name: argo
    repo_url: https://argoproj.github.io/argo-helm
  when: helm_binary_check.rc == 0

- name: Deploy ArgoCD
  kubernetes.core.helm:
    name: argocd
    chart_ref: argo/argo-cd
    release_namespace: "{{ argocd_namespace }}"
    update_repo_cache: true
    create_namespace: true
    chart_version: "{{ argocd_chart_version | default(omit, true) }}"
    kubeconfig: "{{ (ansible_user_dir | default(ansible_env.HOME)) }}/.kube/config"
    values:
      crds:
        install: "{{ argocd_install_crds }}"
        keep: true

      configs:
        cm:
          statusbadge.enabled: true
          resource.customizations.ignoreResourceUpdates.external-secrets.io_ExternalSecret: |
            jqPathExpressions:
              - '.status'
          resource.exclusions: |
            - apiGroups:
              - external-secrets.io
              kinds:
              - PushSecret
        cmp:
          create: true
          plugins:
            kustomize-with-helm:
              generate:
                command: [sh, -c]
                args: ["kustomize build --enable-helm"]

      global:
        logging:
          level: warning

      dex:
        enabled: "{{ argocd_dex_enabled }}"

      redis-ha:
        enabled: "{{ argocd_ha_enabled }}"

      controller:
        replicas: 1
        pdb:
          enabled: false
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            memory: 512Mi
        metrics:
          enabled: true
          serviceMonitor:
            enabled: "{{ argocd_servicemonitor_enabled }}"

      server:
        replicas: 1
        pdb:
          enabled: false
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            memory: 256Mi
        metrics:
          enabled: true
          serviceMonitor:
            enabled: "{{ argocd_servicemonitor_enabled }}"

      repoServer:
        replicas: 1
        pdb:
          enabled: false
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            memory: 256Mi
        metrics:
          enabled: true
          serviceMonitor:
            enabled: "{{ argocd_servicemonitor_enabled }}"

      applicationSet:
        enabled: true
        replicaCount: 1
        pdb:
          enabled: false
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            memory: 256Mi
        metrics:
          enabled: true
          serviceMonitor:
            enabled: "{{ argocd_servicemonitor_enabled }}"

      notifications:
        enabled: true
        pdb:
          enabled: false
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            memory: 128Mi
        metrics:
          enabled: true
          serviceMonitor:
            enabled: "{{ argocd_servicemonitor_enabled }}"
  become: false
  when: helm_binary_check.rc == 0
  tags: ["apps", "argocd"]

- name: Create ArgoCD Admin Password ExternalSecret
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: external-secrets.io/v1
      kind: ExternalSecret
      metadata:
        name: argocd-admin-pass
        namespace: "{{ argocd_namespace }}"
      spec:
        refreshInterval: 1h
        secretStoreRef:
          name: oci-vault
          kind: ClusterSecretStore
        target:
          name: argocd-secret
          creationPolicy: Merge
        data:
          - secretKey: admin.password
            remoteRef:
              key: argocd-admin-pass
    kubeconfig: "{{ (ansible_user_dir | default(ansible_env.HOME)) }}/.kube/config"
  register: argocd_es_created
  become: false
  when: helm_binary_check.rc == 0
  tags: ["apps", "argocd", "secrets"]

- name: Wait for argocd-secret to have admin.password
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    name: argocd-secret
    namespace: "{{ argocd_namespace }}"
    kubeconfig: "{{ (ansible_user_dir | default(ansible_env.HOME)) }}/.kube/config"
  register: argocd_secret_check
  retries: 12
  delay: 5
  until:
    - argocd_secret_check.resources | length > 0
    - "'admin.password' in (argocd_secret_check.resources[0].data | default({}))"
  when: helm_binary_check.rc == 0 and argocd_es_created.changed
  become: false
  tags: ["apps", "argocd", "secrets"]

- name: Restart ArgoCD Server to pick up new password
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: argocd-server
        namespace: "{{ argocd_namespace }}"
        annotations:
          kubectl.kubernetes.io/restartedAt: "{{ ansible_date_time.iso8601 }}"
    kubeconfig: "{{ (ansible_user_dir | default(ansible_env.HOME)) }}/.kube/config"
  when: helm_binary_check.rc == 0 and argocd_es_created.changed
  become: false
  tags: ["apps", "argocd", "secrets"]
