---
galaxy_info:
  description: >-
    Deploy ArgoCD on K3s via Helm (expects External Secrets and an OCI
    ClusterSecretStore named oci-vault for admin password).
  author: rc
  license: MIT
  min_ansible_version: "2.20.0"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - kubernetes
    - k3s
    - argocd

dependencies: []
# NOTE: external_secrets is deployed in Play 1 of site.yaml before argocd
