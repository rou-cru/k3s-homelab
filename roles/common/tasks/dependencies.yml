# Download and verify GitHub CLI repository keyring for secure package installation
- name: Download GitHub CLI keyring
  ansible.builtin.get_url:
    url: https://cli.github.com/packages/githubcli-archive-keyring.gpg
    dest: /usr/share/keyrings/githubcli-archive-keyring.gpg
    mode: '0644'
    force: true
  when: common_install_github_cli_repo | bool
# Verify GPG key fingerprint for GitHub CLI repository security
- name: Verify GitHub CLI keyring fingerprint
  ansible.builtin.command: >-
    gpg --show-keys --with-colons /usr/share/keyrings/githubcli-archive-keyring.gpg
  register: githubcli_keyring_fingerprint
  changed_when: false
  when:
    - common_install_github_cli_repo | bool
    - not ansible_check_mode
# Ensure GitHub CLI keyring matches expected fingerprint for security
- name: Assert GitHub CLI keyring fingerprint
  ansible.builtin.assert:
    that:
      - "'fpr:::::::::2C6106201985B60E6C7AC87323F3D4EA75716059:' in githubcli_keyring_fingerprint.stdout"
    fail_msg: "GitHub CLI keyring fingerprint mismatch."
  when:
    - common_install_github_cli_repo | bool
    - not ansible_check_mode
    - githubcli_keyring_fingerprint is defined
# Add GitHub CLI repository with signed-by option for package verification
- name: Add GitHub CLI repository
  ansible.builtin.apt_repository:
    repo: >-
      {{


        'deb [arch=' ~ ansible_facts['architecture'] ~
        ' signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] ' ~
        'https://cli.github.com/packages stable main'
      }}
    filename: github-cli
    state: present
  when: common_install_github_cli_repo | bool
# Install lightweight debug packages
- name: Install debug packages
  ansible.builtin.apt:
    name: "{{ common_debug_packages }}"
    state: present
    update_cache: true
  when:
    - common_install_debug_packages | bool
    - common_debug_packages | default([]) | length > 0
# Install developer/workstation packages
- name: Install dev packages
  ansible.builtin.apt:
    name: "{{ common_dev_packages }}"
    state: present
    update_cache: true
  when:
    - common_install_dev_packages | bool
    - common_dev_packages | default([]) | length > 0
