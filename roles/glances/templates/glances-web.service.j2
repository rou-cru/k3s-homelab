[Unit]
Description=Glances Web Server
After=network.target

[Service]
Type=simple
# Run as root to access hardware sensors/SMART data without restrictions
User=root
Group=root
ExecStart={{ glances_venv_path }}/bin/glances -w -p {{ glances_port }} -B {{ glances_bind_address }}
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
